dependencies:
  pre:
    - pip install awsebcli
deployment:
  beta:
    branch: master
    commands:
      - rm -r node_modules
      - NODE_ENV=production npm install
      - git add node_modules/ cdn/
      - eb deploy --profile default --staged --debug -v
